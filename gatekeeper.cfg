// GPIO pins numbers (Wiring Pi numbering scheme)
motorPPin      = 0; // Motor pin (through optocoupler)
motorNPin      = 1; // Motor pin (through optocoupler)
ringPin        = 2; // Ring sense pin (through optocoupler)
busSensePin    = 3; // Motor BUS pin (0 means someone's asking for motor to activate)
busActivatePin = 4; // Relay command on Motor BUS


// polarity of the signals and timings                                                                                      
activateBus    = 0; // To activate the bus signal, should we write 0 or 1 ?
busActivationTime = 500; // duration in milliseconds for the bus activation 
                                                                                      
busActivated   = 0; // GPIO state when bus is activated
ringActivated  = 0; // GPIO state when ringer is activated
                                                                                      
loopWaitTime   = 300; //number of millisecs to wait between two polls
